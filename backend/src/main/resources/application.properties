server.port=8080
spring.application.name=indian-stock-analyzer

# CORS settings (only needed when running frontend separately via npm start)
cors.allowed.origins=http://localhost:3000

# Logging
logging.level.com.stockanalyzer=DEBUG
